import { CategorieFilterPipe } from './categorie-filter-pipe';

describe('CategorieFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CategorieFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
